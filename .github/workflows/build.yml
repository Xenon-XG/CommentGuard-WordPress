name: Build & Package Plugin

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build frontend assets
        run: npm run build

      - name: Create plugin zip
        run: |
          mkdir -p dist
          rsync -av --exclude-from='.distignore' ./ dist/commentguard/
          cd dist
          zip -r commentguard.zip commentguard/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: commentguard
          path: dist/commentguard.zip

      - name: Upload to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/commentguard.zip
